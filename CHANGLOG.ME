#変更点
WARNING/CRITICALを指定してなかったら60日/30日をデフォルトにするように変更
どうせwhoisサーバーのレスポンスに統一性がないので関数化して追加変更が簡単にできるように変更
結果表示部分にWARNING/CRITICAL値を表示するように変更
debug_logging()に渡される値が配列の時はvar_dump()で出力するように変更
whois実行失敗の場合の部分を関数化して外出しに変更

#Note
whoisあまり連発するとはじかれるのよなぁ。
