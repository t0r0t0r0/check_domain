#Note
WARNING/CRITICALが指定してなかったら60日/30日をデフォルトにするように変更<br>
どうせwhoisサーバーのレスポンスに統一性がないので関数化して追加変更が簡単にできるように変更<br>
結果表示部分にWARNING/CRITICAL値を表示するように変更<br>
debug_logging()に渡される値が配列の時はvar_dump()で出力するように変更<br>
